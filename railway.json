{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "turbo run build --filter=voice-stream-next"
  },
  "deploy": {
    "startCommand": "turbo run start --filter=voice-stream-next",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 60
  }
}
