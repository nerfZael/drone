# drone-sdk

TypeScript SDK for creating and operating drones through Drone Hub.

Current package surface:

- `createDroneSDK(...)`
- `hubTransport(...)`
- `createMockTransport(...)`

See the top-level spec at `DRONE_SDK_MVP_SPEC.md` for the intended MVP surface and semantics.
