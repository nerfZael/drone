diff --git a/android/src/main/java/expo/modules/audio/AudioModule.kt b/android/src/main/java/expo/modules/audio/AudioModule.kt
index 992239b..3b52466 100644
--- a/android/src/main/java/expo/modules/audio/AudioModule.kt
+++ b/android/src/main/java/expo/modules/audio/AudioModule.kt
@@ -652,6 +652,7 @@ class AudioModule : Module() {
 
       AsyncFunction("start") Coroutine { stream: AudioStream ->
         checkRecordingPermission()
+        stream.useForegroundService = allowsBackgroundRecording
         stream.start()
       }
 
diff --git a/android/src/main/java/expo/modules/audio/AudioRecorder.kt b/android/src/main/java/expo/modules/audio/AudioRecorder.kt
index 9a76836..b66f74b 100644
--- a/android/src/main/java/expo/modules/audio/AudioRecorder.kt
+++ b/android/src/main/java/expo/modules/audio/AudioRecorder.kt
@@ -14,6 +14,7 @@ import android.os.Bundle
 import androidx.core.content.ContextCompat
 import androidx.core.net.toUri
 import expo.modules.audio.service.AudioRecordingServiceConnection
+import expo.modules.audio.service.BackgroundAudioRecorder
 import expo.modules.kotlin.AppContext
 import expo.modules.kotlin.exception.CodedException
 import expo.modules.kotlin.exception.Exceptions
@@ -35,7 +36,8 @@ class AudioRecorder(
   private val options: RecordingOptions
 ) : SharedObject(appContext),
   MediaRecorder.OnErrorListener,
-  MediaRecorder.OnInfoListener {
+  MediaRecorder.OnInfoListener,
+  BackgroundAudioRecorder {
   var filePath: String? = null
   private var meteringEnabled = options.isMeteringEnabled
   private var durationAlreadyRecorded = 0L
@@ -48,8 +50,10 @@ class AudioRecorder(
   var isPaused = false
   private var recordingTimerJob: Job? = null
   var useForegroundService = false
-
-  val serviceConnection = AudioRecordingServiceConnection(WeakReference(this), appContext)
+  val serviceConnection = AudioRecordingServiceConnection(
+    WeakReference<BackgroundAudioRecorder>(this),
+    appContext
+  )
 
   private val _appContext: AppContext
     get() {
@@ -85,10 +89,6 @@ class AudioRecorder(
       throw AudioRecorderAlreadyPreparedException()
     }
 
-    if (useForegroundService && !hasNotificationPermissions()) {
-      throw NotificationPermissionsException()
-    }
-
     val recordingOptions = options ?: this.options
     val mediaRecorder = createRecorder(recordingOptions)
     recorder = mediaRecorder
@@ -208,6 +208,10 @@ class AudioRecorder(
     return status
   }
 
+  override fun stopBackgroundRecording() {
+    stopRecording()
+  }
+
   private fun reset() {
     recordingTimerJob?.cancel()
     recordingTimerJob = null
@@ -430,14 +434,6 @@ class AudioRecorder(
   private fun hasRecordingPermissions() =
     ContextCompat.checkSelfPermission(context, Manifest.permission.RECORD_AUDIO) == PackageManager.PERMISSION_GRANTED
 
-  private fun hasNotificationPermissions(): Boolean {
-    // POST_NOTIFICATIONS permission is only required on Android 13+ (API 33+)
-    if (Build.VERSION.SDK_INT < Build.VERSION_CODES.TIRAMISU) {
-      return true
-    }
-    return ContextCompat.checkSelfPermission(context, Manifest.permission.POST_NOTIFICATIONS) == PackageManager.PERMISSION_GRANTED
-  }
-
   fun getAvailableInputs(audioManager: AudioManager) =
     audioManager.getDevices(AudioManager.GET_DEVICES_INPUTS).mapNotNull { deviceInfo ->
       val type = deviceInfo.type
diff --git a/android/src/main/java/expo/modules/audio/AudioStream.kt b/android/src/main/java/expo/modules/audio/AudioStream.kt
index 8ecdcc1..3c88c14 100644
--- a/android/src/main/java/expo/modules/audio/AudioStream.kt
+++ b/android/src/main/java/expo/modules/audio/AudioStream.kt
@@ -3,8 +3,13 @@ package expo.modules.audio
 import android.Manifest
 import android.media.AudioFormat
 import android.media.AudioRecord
+import android.media.AudioRecordingConfiguration
+import android.media.AudioManager
 import android.media.MediaRecorder
+import android.os.Build
 import androidx.annotation.RequiresPermission
+import expo.modules.audio.service.AudioRecordingServiceConnection
+import expo.modules.audio.service.BackgroundAudioRecorder
 import expo.modules.kotlin.AppContext
 import expo.modules.kotlin.jni.NativeArrayBuffer
 import expo.modules.kotlin.sharedobjects.SharedObject
@@ -15,6 +20,7 @@ import kotlinx.coroutines.isActive
 import kotlinx.coroutines.launch
 import java.nio.ByteBuffer
 import java.nio.ByteOrder
+import java.lang.ref.WeakReference
 
 internal const val AUDIO_STREAM_BUFFER = "audioStreamBuffer"
 internal const val AUDIO_STREAM_STATUS = "audioStreamStatus"
@@ -34,7 +40,7 @@ private data class ResolvedAudioConfig(
 class AudioStream(
   appContext: AppContext,
   private val options: AudioStreamOptions
-) : SharedObject(appContext) {
+) : SharedObject(appContext), BackgroundAudioRecorder {
 
   val id: String = java.util.UUID.randomUUID().toString()
 
@@ -50,13 +56,27 @@ class AudioStream(
     private set
 
   private var startTimeNanos: Long = 0
+  private var startRequestGeneration = 0
+  private var recordingCallback: AudioManager.AudioRecordingCallback? = null
+  private var clientSilenced = false
+  var useForegroundService = false
+  private val serviceConnection = AudioRecordingServiceConnection(
+    WeakReference<BackgroundAudioRecorder>(this),
+    appContext
+  )
 
   @RequiresPermission(Manifest.permission.RECORD_AUDIO)
-  fun start() {
+  suspend fun start() {
     if (isStreaming) {
       return
     }
 
+    val requestGeneration = ++startRequestGeneration
+    if (useForegroundService) {
+      serviceConnection.bindWithService()
+    }
+    if (requestGeneration != startRequestGeneration) return
+
     val config = resolveAudioConfig()
     val recorder = createAudioRecord(config)
 
@@ -74,20 +94,68 @@ class AudioStream(
     audioRecord = recorder
     startTimeNanos = System.nanoTime()
     isStreaming = true
-    emitStatus()
+    if (useForegroundService) {
+      serviceConnection.recordingServiceBinder?.service?.registerRecorder(this) ?: run {
+        recorder.stop()
+        recorder.release()
+        audioRecord = null
+        isStreaming = false
+        throw AudioRecordingServiceException("The service connection is not bound, but `allowsBackgroundRecording` is set to `true`")
+      }
+    }
+    observeTemporaryMicrophoneLoss(recorder)
+    emitStatus("started")
 
     startCaptureLoop(recorder, config)
   }
 
-  fun stop() {
+  fun stop(reason: String = "stopped") {
+    startRequestGeneration += 1
     if (!isStreaming) return
     isStreaming = false
+    if (useForegroundService) {
+      serviceConnection.recordingServiceBinder?.service?.unregisterRecorder(this)
+    }
     captureJob?.cancel()
     captureJob = null
+    stopObservingTemporaryMicrophoneLoss(audioRecord)
     audioRecord?.stop()
     audioRecord?.release()
     audioRecord = null
-    emitStatus()
+    emitStatus(reason)
+  }
+
+  override fun stopBackgroundRecording() {
+    stop("system-control")
+  }
+
+  private fun observeTemporaryMicrophoneLoss(recorder: AudioRecord) {
+    if (Build.VERSION.SDK_INT < Build.VERSION_CODES.Q) return
+    val context = appContext?.reactContext ?: return
+    clientSilenced = false
+    val callback = object : AudioManager.AudioRecordingCallback() {
+      override fun onRecordingConfigChanged(configs: MutableList<AudioRecordingConfiguration>) {
+        val configuration = configs.firstOrNull {
+          it.clientAudioSessionId == recorder.audioSessionId
+        } ?: return
+        val nextSilenced = configuration.isClientSilenced
+        if (!isStreaming || nextSilenced == clientSilenced) return
+        clientSilenced = nextSilenced
+        emitStatus(if (nextSilenced) "interrupted" else "started")
+      }
+    }
+    recordingCallback = callback
+    recorder.registerAudioRecordingCallback(context.mainExecutor, callback)
+  }
+
+  private fun stopObservingTemporaryMicrophoneLoss(recorder: AudioRecord?) {
+    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
+      recordingCallback?.let { callback ->
+        recorder?.unregisterAudioRecordingCallback(callback)
+      }
+    }
+    recordingCallback = null
+    clientSilenced = false
   }
 
   private fun resolveAudioConfig(): ResolvedAudioConfig {
@@ -182,11 +250,11 @@ class AudioStream(
     }
   }
 
-  private fun emitStatus() {
+  private fun emitStatus(reason: String) {
     appContext?.mainQueue?.launch {
       emit(
         AUDIO_STREAM_STATUS,
-        mapOf("isStreaming" to isStreaming)
+        mapOf("isStreaming" to isStreaming, "reason" to reason)
       )
     }
   }
@@ -210,5 +278,10 @@ class AudioStream(
   override fun sharedObjectDidRelease() {
     super.sharedObjectDidRelease()
     stop()
+    serviceConnection.release()
+    if (useForegroundService) {
+      serviceConnection.unbind()
+      serviceConnection.cleanup()
+    }
   }
 }
diff --git a/android/src/main/java/expo/modules/audio/service/AudioRecordingService.kt b/android/src/main/java/expo/modules/audio/service/AudioRecordingService.kt
index 2c92303..f9722f1 100644
--- a/android/src/main/java/expo/modules/audio/service/AudioRecordingService.kt
+++ b/android/src/main/java/expo/modules/audio/service/AudioRecordingService.kt
@@ -12,7 +12,6 @@ import android.os.Build
 import android.os.IBinder
 import android.util.Log
 import androidx.core.app.NotificationCompat
-import expo.modules.audio.AudioRecorder
 import expo.modules.audio.getRecordingServiceErrorMessage
 import expo.modules.kotlin.AppContext
 import java.lang.ref.WeakReference
@@ -21,7 +20,7 @@ private const val TAG = "AudioRecordingService"
 
 class AudioRecordingService : Service() {
   private val binder = AudioRecordingServiceBinder(this)
-  private val activeRecorders = mutableSetOf<WeakReference<AudioRecorder>>()
+  private val activeRecorders = mutableSetOf<WeakReference<BackgroundAudioRecorder>>()
   private val recorderLock = Any()
   private var notificationId = NOTIFICATION_ID
 
@@ -96,8 +95,8 @@ class AudioRecordingService : Service() {
     }
 
     return NotificationCompat.Builder(this, CHANNEL_ID)
-      .setContentTitle("Recording audio")
-      .setContentText("Tap to return to app")
+      .setContentTitle("Drone Hub is recording audio")
+      .setContentText("Tap to return to Drone Hub")
       .setSmallIcon(android.R.drawable.ic_btn_speak_now)
       .setOngoing(true)
       .setContentIntent(contentIntent)
@@ -141,7 +140,7 @@ class AudioRecordingService : Service() {
     isRunningForeground = false
   }
 
-  fun registerRecorder(recorder: AudioRecorder) {
+  fun registerRecorder(recorder: BackgroundAudioRecorder) {
     synchronized(recorderLock) {
       activeRecorders.removeAll { it.get() == null }
 
@@ -157,7 +156,7 @@ class AudioRecordingService : Service() {
     }
   }
 
-  fun unregisterRecorder(recorder: AudioRecorder) {
+  fun unregisterRecorder(recorder: BackgroundAudioRecorder) {
     synchronized(recorderLock) {
       activeRecorders.removeAll { it.get() == recorder || it.get() == null }
 
@@ -169,12 +168,12 @@ class AudioRecordingService : Service() {
   }
 
   private fun stopRecordingAndService() {
-    synchronized(recorderLock) {
-      activeRecorders.forEach { weakRef ->
-        weakRef.get()?.stopRecording()
-      }
+    val recorders = synchronized(recorderLock) {
+      val snapshot = activeRecorders.mapNotNull { it.get() }
       activeRecorders.clear()
+      snapshot
     }
+    recorders.forEach { it.stopBackgroundRecording() }
 
     stopForegroundWithNotification()
     stopSelf()
@@ -200,3 +199,8 @@ class AudioRecordingService : Service() {
     internal const val ACTION_STOP_RECORDING = "expo.modules.audio.action.STOP_RECORDING"
   }
 }
+
+/** A microphone capture that is kept alive by the recording foreground service. */
+interface BackgroundAudioRecorder {
+  fun stopBackgroundRecording()
+}
diff --git a/android/src/main/java/expo/modules/audio/service/AudioRecordingServiceConnection.kt b/android/src/main/java/expo/modules/audio/service/AudioRecordingServiceConnection.kt
index 28be72f..e3ef62f 100644
--- a/android/src/main/java/expo/modules/audio/service/AudioRecordingServiceConnection.kt
+++ b/android/src/main/java/expo/modules/audio/service/AudioRecordingServiceConnection.kt
@@ -2,25 +2,21 @@ package expo.modules.audio.service
 
 import android.content.ComponentName
 import android.os.IBinder
-import expo.modules.audio.AudioRecorder
 import expo.modules.audio.AudioRecordingServiceException
 import expo.modules.audio.getRecordingServiceErrorMessage
 import expo.modules.kotlin.AppContext
+import kotlinx.coroutines.CompletableDeferred
 import kotlinx.coroutines.CoroutineScope
 import kotlinx.coroutines.Dispatchers
 import kotlinx.coroutines.Job
 import kotlinx.coroutines.delay
 import kotlinx.coroutines.launch
 import java.lang.ref.WeakReference
-import kotlin.coroutines.Continuation
-import kotlin.coroutines.resume
-import kotlin.coroutines.resumeWithException
-import kotlin.coroutines.suspendCoroutine
 
 class AudioRecordingServiceBinder(val service: AudioRecordingService) : android.os.Binder()
 
 class AudioRecordingServiceConnection(
-  val recorder: WeakReference<AudioRecorder>,
+  val recorder: WeakReference<BackgroundAudioRecorder>,
   appContext: AppContext
 ) : BaseServiceConnection<AudioRecordingServiceBinder>(appContext) {
   var recordingServiceBinder: AudioRecordingServiceBinder? = null
@@ -29,21 +25,38 @@ class AudioRecordingServiceConnection(
 
   @Volatile
   private var isRecorderReleased = false
-  private var bindingContinuation: Continuation<Unit>? = null
+  private var pendingBinding: CompletableDeferred<Unit>? = null
 
   fun release() {
-    isRecorderReleased = true
-    cancelBindingTimeout()
+    val pending = synchronized(this) {
+      isRecorderReleased = true
+      cancelBindingTimeout()
+      if (bindingState == ServiceBindingState.BINDING) {
+        unbind()
+      }
+      takePendingBinding()
+    }
+    pending?.completeExceptionally(
+      AudioRecordingServiceException("The recorder has been deallocated")
+    )
   }
 
   fun startBindingTimeout(timeoutMs: Long = 5000) { // 5000ms is more or less the default Android system timeout for binding
     bindingTimeoutJob?.cancel()
     bindingTimeoutJob = CoroutineScope(Dispatchers.Default).launch {
       delay(timeoutMs)
-      if (bindingState == ServiceBindingState.BINDING) {
-        bindingContinuation?.resumeWithException(AudioRecordingServiceException("The recording service connection has failed to connect with the recording service within ${timeoutMs}ms"))
+      val pending = synchronized(this@AudioRecordingServiceConnection) {
+        if (bindingState != ServiceBindingState.BINDING) return@launch
+        // Mark the attempt unbound while holding the state lock so a late
+        // onServiceConnected callback cannot revive this timed-out binding.
         unbind()
+        takePendingBinding()
       }
+      pending?.completeExceptionally(
+        AudioRecordingServiceException(
+          "The recording service connection has failed to connect with the recording service within ${timeoutMs}ms"
+        )
+      )
     }
   }
 
@@ -52,90 +65,171 @@ class AudioRecordingServiceConnection(
     bindingTimeoutJob = null
   }
 
-  /**
-   * Binds with the recording service. If the service doesn't exist, it will create the service.
-   */
-  suspend fun bindWithService() {
-    if (bindingState == ServiceBindingState.BOUND) {
-      return
-    }
+  private fun takePendingBinding(): CompletableDeferred<Unit>? {
+    val pending = pendingBinding
+    pendingBinding = null
+    return pending
+  }
 
-    if (bindingState == ServiceBindingState.BINDING) {
-      throw AudioRecordingServiceException("Tried binding to the recording service while the previous attempt is still ongoing.")
+  private fun failPendingBinding(
+    exception: AudioRecordingServiceException,
+    nextState: ServiceBindingState? = null
+  ) {
+    val pending = synchronized(this) {
+      nextState?.let(::transitionToState)
+      takePendingBinding()
     }
+    pending?.completeExceptionally(exception)
+  }
 
-    if (bindingState == ServiceBindingState.UNBINDING) {
-      throw AudioRecordingServiceException("Tried binding to the recording service while the unbinding process is ongoing.")
+  /**
+   * Binds with the recording service. Concurrent callers await the same native
+   * attempt instead of starting overlapping binds.
+   */
+  suspend fun bindWithService() {
+    val (pending, shouldStart) = synchronized(this) {
+      if (isRecorderReleased) {
+        throw AudioRecordingServiceException("The recorder has been deallocated")
+      }
+      when (bindingState) {
+        ServiceBindingState.BOUND -> return
+        ServiceBindingState.BINDING -> {
+          val active = pendingBinding
+            ?: throw AudioRecordingServiceException("The recording service binding state is inconsistent")
+          active to false
+        }
+        ServiceBindingState.UNBINDING -> throw AudioRecordingServiceException(
+          "Tried binding to the recording service while the unbinding process is ongoing."
+        )
+        ServiceBindingState.UNBOUND, ServiceBindingState.FAILED -> {
+          val created = CompletableDeferred<Unit>()
+          pendingBinding = created
+          transitionToState(ServiceBindingState.BINDING)
+          created to true
+        }
+      }
     }
 
-    suspendCoroutine { continuation ->
-      if (bindingState == ServiceBindingState.UNBOUND || bindingState == ServiceBindingState.FAILED) {
-        val reactContext = appContext.reactContext ?: run {
-          onBindingFailed("Binding with the expo-audio playback service failed: React context lost")
-          return@suspendCoroutine
+    if (shouldStart) {
+      val reactContext = appContext.reactContext
+      if (reactContext == null) {
+        val exception = AudioRecordingServiceException(
+          "Binding with the recording service failed: React context lost"
+        )
+        onBindingFailed(exception.message ?: "React context lost", exception)
+        failPendingBinding(exception, ServiceBindingState.FAILED)
+      } else {
+        val serviceRunning = try {
+          synchronized(this) {
+            if (isRecorderReleased || bindingState != ServiceBindingState.BINDING) {
+              false
+            } else {
+              startServiceAndBind(
+                appContext,
+                reactContext,
+                this,
+                AudioRecordingService::class.java,
+                AudioRecordingService.ACTION_START_RECORDING
+              )
+            }
+          }
+        } catch (exception: Exception) {
+          failPendingBinding(
+            AudioRecordingServiceException(exception.message, exception),
+            ServiceBindingState.FAILED
+          )
+          false
         }
-        val serviceRunning = startServiceAndBind(appContext, reactContext, this, AudioRecordingService::class.java, AudioRecordingService.ACTION_START_RECORDING)
 
-        if (!serviceRunning) {
-          continuation.resumeWithException(AudioRecordingServiceException("Failed to start the recording service"))
-          return@suspendCoroutine
+        if (!serviceRunning && !pending.isCompleted) {
+          failPendingBinding(
+            AudioRecordingServiceException("Failed to start the recording service"),
+            ServiceBindingState.FAILED
+          )
+        } else if (serviceRunning && bindingState == ServiceBindingState.BINDING) {
+          startBindingTimeout()
         }
-
-        transitionToState(ServiceBindingState.BINDING)
-        bindingContinuation = continuation
       }
     }
+
+    pending.await()
   }
 
   override fun onServiceConnected(componentName: ComponentName, binder: IBinder) {
     cancelBindingTimeout()
 
-    val recorder = recorder.get()
-    if (recorder == null || isRecorderReleased) {
-      transitionToState(ServiceBindingState.FAILED)
-      bindingContinuation?.resumeWithException(AudioRecordingServiceException("The recorder has been deallocated"))
-      return
+    val completed = synchronized(this) {
+      // Ignore callbacks from attempts that were released, timed out, or otherwise
+      // failed after Android had already queued the connection callback.
+      if (bindingState != ServiceBindingState.BINDING || isRecorderReleased) {
+        return@synchronized null
+      }
+      if (recorder.get() == null) {
+        unbind()
+        transitionToState(ServiceBindingState.FAILED)
+        return@synchronized takePendingBinding() to
+          AudioRecordingServiceException("The recorder has been deallocated")
+      }
+      val serviceBinder = binder as? AudioRecordingServiceBinder
+      if (serviceBinder == null) {
+        unbind()
+        transitionToState(ServiceBindingState.FAILED)
+        return@synchronized takePendingBinding() to
+          AudioRecordingServiceException("Could not bind to the recording service - invalid binder type")
+      }
+      recordingServiceBinder = serviceBinder
+      serviceBinder.service.appContext = appContext
+      transitionToState(ServiceBindingState.BOUND)
+      takePendingBinding() to null
     }
 
-    val serviceBinder: AudioRecordingServiceBinder = binder as? AudioRecordingServiceBinder ?: run {
-      bindingContinuation?.resumeWithException(AudioRecordingServiceException("Could not bind to the recording service - invalid binder type"))
-      transitionToState(ServiceBindingState.FAILED)
-      return
+    val (pending, exception) = completed ?: return
+    if (exception == null) {
+      pending?.complete(Unit)
+    } else {
+      pending?.completeExceptionally(exception)
     }
-    transitionToState(ServiceBindingState.BOUND)
-
-    bindingContinuation?.resume(Unit)
-    recordingServiceBinder = serviceBinder
-    serviceBinder.service.appContext = appContext
   }
 
   override fun onServiceDisconnected(componentName: ComponentName) {
     cancelBindingTimeout()
     recordingServiceBinder = null
-    super.onServiceDisconnected(componentName)
+    failPendingBinding(
+      AudioRecordingServiceException("Service disconnected unexpectedly"),
+      ServiceBindingState.UNBOUND
+    )
+    super.onServiceDied()
   }
 
   override fun onBindingDied(name: ComponentName?) {
     cancelBindingTimeout()
-    bindingContinuation?.resumeWithException(
-      AudioRecordingServiceException("Service binding died")
+    recordingServiceBinder = null
+    failPendingBinding(
+      AudioRecordingServiceException("Service binding died"),
+      ServiceBindingState.UNBOUND
     )
-    bindingContinuation = null
-    super.onBindingDied(name)
+    super.onServiceDied()
   }
 
   override fun onNullBinding(componentName: ComponentName) {
     cancelBindingTimeout()
-    bindingContinuation?.resumeWithException(
-      AudioRecordingServiceException("Service returned null binding")
+    recordingServiceBinder = null
+    failPendingBinding(
+      AudioRecordingServiceException("Service returned null binding"),
+      ServiceBindingState.FAILED
+    )
+    onBindingFailed(
+      "Could not bind to the service. Check AndroidManifest.xml for proper service and permission declarations."
     )
-    bindingContinuation = null
-    super.onNullBinding(componentName)
   }
 
   override fun onServiceDied() {
     cancelBindingTimeout()
     recordingServiceBinder = null
+    failPendingBinding(
+      AudioRecordingServiceException("Service died unexpectedly"),
+      ServiceBindingState.UNBOUND
+    )
     super.onServiceDied()
   }
 
diff --git a/build/AudioStream.js b/build/AudioStream.js
index 2354b36..9f73e40 100644
--- a/build/AudioStream.js
+++ b/build/AudioStream.js
@@ -8,10 +8,12 @@ import AudioModule from './AudioModule';
  * Requires microphone permission via `requestRecordingPermissionsAsync()`.
  */
 export function useAudioStream(options = {}) {
-    const { sampleRate = 48000, channels = 1, encoding = 'float32', onBuffer } = options;
+    const { sampleRate = 48000, channels = 1, encoding = 'float32', onBuffer, onStatus } = options;
     const [isStreaming, setIsStreaming] = useState(false);
     const onBufferRef = useRef(onBuffer);
     onBufferRef.current = onBuffer;
+    const onStatusRef = useRef(onStatus);
+    onStatusRef.current = onStatus;
     const stream = useReleasingSharedObject(() => new AudioModule.AudioStream({
         sampleRate,
         channels,
@@ -20,6 +22,7 @@ export function useAudioStream(options = {}) {
     useEffect(() => {
         const statusSub = stream.addListener(AUDIO_STREAM_STATUS, (status) => {
             setIsStreaming(status.isStreaming);
+            onStatusRef.current?.(status);
         });
         const bufferSub = stream.addListener(AUDIO_STREAM_BUFFER, (buffer) => {
             onBufferRef.current?.(buffer);
@@ -31,4 +34,4 @@ export function useAudioStream(options = {}) {
     }, [stream.id]);
     return { stream, isStreaming };
 }
-//# sourceMappingURL=AudioStream.js.map
\ No newline at end of file
+//# sourceMappingURL=AudioStream.js.map
diff --git a/build/AudioStream.types.d.ts b/build/AudioStream.types.d.ts
index da91d06..155bb8b 100644
--- a/build/AudioStream.types.d.ts
+++ b/build/AudioStream.types.d.ts
@@ -21,6 +21,8 @@ export type AudioStreamOptions = {
      * Called each time a new PCM buffer is captured from the microphone.
      */
     onBuffer?: (buffer: AudioStreamBuffer) => void;
+    /** Called when capture starts, stops, or is temporarily interrupted. */
+    onStatus?: (status: AudioStreamStatus) => void;
 };
 export type AudioStreamBuffer = {
     /**
@@ -40,6 +42,7 @@ export type AudioStreamBuffer = {
 };
 export type AudioStreamStatus = {
     isStreaming: boolean;
+    reason?: 'started' | 'stopped' | 'interrupted' | 'system-control';
 };
 export type AudioStreamEvents = {
     audioStreamBuffer(buffer: AudioStreamBuffer): void;
@@ -73,4 +76,4 @@ export declare class AudioStream extends SharedObject<AudioStreamEvents> {
     /** Stops capturing and releases native audio resources. */
     stop(): void;
 }
-//# sourceMappingURL=AudioStream.types.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=AudioStream.types.d.ts.map
diff --git a/ios/AudioStream.swift b/ios/AudioStream.swift
index f938eff..5e463da 100644
--- a/ios/AudioStream.swift
+++ b/ios/AudioStream.swift
@@ -18,15 +18,33 @@ class AudioStream: SharedObject {
   private(set) var channels: Int = 0
   private(set) var isStreaming: Bool = false
   private var startTimestamp: AVAudioTime?
+  private var wantsToStream = false
+  private var recoveryAttempt = 0
+  private var recoveryWorkItem: DispatchWorkItem?
+  private var notificationObservers = [NSObjectProtocol]()
 
   init(options: AudioStreamOptions) {
     self.requestedSampleRate = options.sampleRate
     self.requestedChannels = options.channels
     self.encoding = options.encoding
     super.init()
+    setupRecoveryObservers()
   }
 
   func start() throws {
+    let wasWanted = wantsToStream
+    wantsToStream = true
+    do {
+      try startEngine()
+      recoveryAttempt = 0
+    } catch {
+      wantsToStream = wasWanted
+      try? AVAudioSession.sharedInstance().setActive(false, options: .notifyOthersOnDeactivation)
+      throw error
+    }
+  }
+
+  private func startEngine() throws {
     guard !isStreaming else { return }
 
     let session = AVAudioSession.sharedInstance()
@@ -61,14 +79,9 @@ class AudioStream: SharedObject {
 
     if needsConversion {
       guard let conv = AVAudioConverter(from: hardwareFormat, to: targetFormat) else {
-        sampleRate = hardwareFormat.sampleRate
-        channels = Int(hardwareFormat.channelCount)
-        setupTapWithoutConversion(engine: engine, inputNode: inputNode, format: hardwareFormat)
-        self.audioEngine = engine
-        try engine.start()
-        isStreaming = true
-        emitStatus()
-        return
+        throw AudioStreamException(
+          "The microphone format cannot be converted to the requested streaming format."
+        )
       }
       self.converter = conv
     }
@@ -94,22 +107,108 @@ class AudioStream: SharedObject {
     }
 
     self.audioEngine = engine
-    try engine.start()
+    do {
+      try engine.start()
+    } catch {
+      inputNode.removeTap(onBus: 0)
+      self.audioEngine = nil
+      self.converter = nil
+      throw error
+    }
     isStreaming = true
-    emitStatus()
+    emitStatus(reason: "started")
   }
 
   func stop() {
-    guard isStreaming else { return }
-    audioEngine?.inputNode.removeTap(onBus: 0)
-    audioEngine?.stop()
-    audioEngine = nil
-    converter = nil
-    isStreaming = false
-    startTimestamp = nil
-    emitStatus()
+    wantsToStream = false
+    recoveryWorkItem?.cancel()
+    recoveryWorkItem = nil
+    stopEngine(deactivateSession: true, reason: "stopped")
+  }
+
+  private func stopEngine(deactivateSession: Bool, reason: String) {
+    if isStreaming {
+      audioEngine?.inputNode.removeTap(onBus: 0)
+      audioEngine?.stop()
+      audioEngine = nil
+      converter = nil
+      isStreaming = false
+      startTimestamp = nil
+      emitStatus(reason: reason)
+    }
+
+    if deactivateSession {
+      try? AVAudioSession.sharedInstance().setActive(false, options: .notifyOthersOnDeactivation)
+    }
+  }
+
+  private func setupRecoveryObservers() {
+    let center = NotificationCenter.default
+    notificationObservers.append(center.addObserver(
+      forName: AVAudioSession.interruptionNotification,
+      object: AVAudioSession.sharedInstance(),
+      queue: .main
+    ) { [weak self] notification in
+      self?.handleInterruption(notification)
+    })
+    notificationObservers.append(center.addObserver(
+      forName: .AVAudioEngineConfigurationChange,
+      object: nil,
+      queue: .main
+    ) { [weak self] _ in
+      self?.recoverAfterTemporaryAudioLoss()
+    })
+    notificationObservers.append(center.addObserver(
+      forName: AVAudioSession.routeChangeNotification,
+      object: AVAudioSession.sharedInstance(),
+      queue: .main
+    ) { [weak self] _ in
+      self?.scheduleRecovery()
+    })
+    notificationObservers.append(center.addObserver(
+      forName: AVAudioSession.mediaServicesWereResetNotification,
+      object: AVAudioSession.sharedInstance(),
+      queue: .main
+    ) { [weak self] _ in
+      self?.recoverAfterTemporaryAudioLoss()
+    })
+  }
 
-    try? AVAudioSession.sharedInstance().setActive(false, options: .notifyOthersOnDeactivation)
+  private func handleInterruption(_ notification: Notification) {
+    guard wantsToStream,
+      let rawValue = notification.userInfo?[AVAudioSessionInterruptionTypeKey] as? UInt,
+      let type = AVAudioSession.InterruptionType(rawValue: rawValue) else {
+      return
+    }
+    if type == .began {
+      stopEngine(deactivateSession: false, reason: "interrupted")
+      return
+    }
+    scheduleRecovery()
+  }
+
+  private func recoverAfterTemporaryAudioLoss() {
+    guard wantsToStream else { return }
+    stopEngine(deactivateSession: false, reason: "interrupted")
+    scheduleRecovery()
+  }
+
+  private func scheduleRecovery() {
+    recoveryWorkItem?.cancel()
+    guard wantsToStream, !isStreaming else { return }
+    let delay = min(0.5 * pow(2.0, Double(recoveryAttempt)), 15.0)
+    let work = DispatchWorkItem { [weak self] in
+      guard let self, self.wantsToStream, !self.isStreaming else { return }
+      do {
+        try self.startEngine()
+        self.recoveryAttempt = 0
+      } catch {
+        self.recoveryAttempt += 1
+        self.scheduleRecovery()
+      }
+    }
+    recoveryWorkItem = work
+    DispatchQueue.main.asyncAfter(deadline: .now() + delay, execute: work)
   }
 
   private func setupTapWithoutConversion(engine: AVAudioEngine, inputNode: AVAudioInputNode, format: AVAudioFormat) {
@@ -154,9 +253,10 @@ class AudioStream: SharedObject {
     }
   }
 
-  private func emitStatus() {
+  private func emitStatus(reason: String) {
     emit(event: AUDIO_STREAM_STATUS, payload: [
-      "isStreaming": isStreaming
+      "isStreaming": isStreaming,
+      "reason": reason
     ])
   }
 
@@ -195,6 +295,7 @@ class AudioStream: SharedObject {
   }
 
   deinit {
+    notificationObservers.forEach { NotificationCenter.default.removeObserver($0) }
     stop()
   }
 }
diff --git a/src/AudioStream.ts b/src/AudioStream.ts
index e93e8d9..aa4b640 100644
--- a/src/AudioStream.ts
+++ b/src/AudioStream.ts
@@ -16,10 +16,12 @@ import type {
  * Requires microphone permission via `requestRecordingPermissionsAsync()`.
  */
 export function useAudioStream(options: AudioStreamOptions = {}): AudioStreamResult {
-  const { sampleRate = 48000, channels = 1, encoding = 'float32', onBuffer } = options;
+  const { sampleRate = 48000, channels = 1, encoding = 'float32', onBuffer, onStatus } = options;
   const [isStreaming, setIsStreaming] = useState(false);
   const onBufferRef = useRef(onBuffer);
   onBufferRef.current = onBuffer;
+  const onStatusRef = useRef(onStatus);
+  onStatusRef.current = onStatus;
 
   const stream = useReleasingSharedObject(
     () =>
@@ -34,6 +36,7 @@ export function useAudioStream(options: AudioStreamOptions = {}): AudioStreamRes
   useEffect(() => {
     const statusSub = stream.addListener(AUDIO_STREAM_STATUS, (status: AudioStreamStatus) => {
       setIsStreaming(status.isStreaming);
+      onStatusRef.current?.(status);
     });
 
     const bufferSub = stream.addListener(AUDIO_STREAM_BUFFER, (buffer: AudioStreamBuffer) => {
diff --git a/src/AudioStream.types.ts b/src/AudioStream.types.ts
index 1f4bc46..02b9af2 100644
--- a/src/AudioStream.types.ts
+++ b/src/AudioStream.types.ts
@@ -26,6 +26,9 @@ export type AudioStreamOptions = {
    * Called each time a new PCM buffer is captured from the microphone.
    */
   onBuffer?: (buffer: AudioStreamBuffer) => void;
+
+  /** Called when capture starts, stops, or is temporarily interrupted. */
+  onStatus?: (status: AudioStreamStatus) => void;
 };
 
 export type AudioStreamBuffer = {
@@ -50,6 +53,7 @@ export type AudioStreamBuffer = {
 
 export type AudioStreamStatus = {
   isStreaming: boolean;
+  reason?: 'started' | 'stopped' | 'interrupted' | 'system-control';
 };
 
 export type AudioStreamEvents = {
